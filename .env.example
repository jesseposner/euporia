# Database (required for sqlx compile-time query checking)
DATABASE_URL=sqlite:euporia.db

# BDK Wallet (signet)
# BDK_DESCRIPTOR=wpkh(...)
# BDK_CHANGE_DESCRIPTOR=wpkh(...)

# LLM provider: "anthropic" (default) or "local" (llama-server)
# LLM_PROVIDER=local
# LLM_LOCAL_URL=http://localhost:8080/v1
# LLM_LOCAL_MODEL=qwen-coder-3
